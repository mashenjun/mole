# mole
```shell
❯ ./mole help
mole is a command-line to collect information and data masking

Usage:
  mole [flags]
  mole [command]

Available Commands:
  convert     convert to csv file
  1 # mole
  help        Help about any command
  keyviz      collect heatmap from dashboard api
  metrics     collect metrics from target prometheus
  reshape     reshape multiple metrics to csv files
  schema      collect schema from target databases
  split       split heatmap data to multiple csv file

Flags:
  -h, --help   help for mole

Use "mole [command] --help" for more information about a command.
```
